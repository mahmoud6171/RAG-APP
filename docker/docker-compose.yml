services:
  mongodb:
    image: mongo:noble

    container_name: mongodb

    ports: 
      - 27017:27017
    
    volumes:
      - ./mongodb:/data/db
    
    networks:
      - Backend

    restart: always

networks:
  Backend: